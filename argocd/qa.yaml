apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: crud-qa
  namespace: argocd

spec:
  project: default

  source:
    repoURL: https://github.com/YOUR_USERNAME/crud-app-gitops.git
    targetRevision: main
    path: overlays/qa

  destination:
    server: https://kubernetes.default.svc
    namespace: crud-qa

  syncPolicy:
    automated:
      prune: true
      selfHeal: true